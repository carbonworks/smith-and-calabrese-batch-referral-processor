package tech.carbonworks.snc.batchreferralparser.util

/**
 * Compile-time build configuration constants.
 *
 * [DEBUG] is set to `true` for development builds and controls PHI masking
 * in the UI. Release builds will be configured to set this to `false`.
 */
object BuildConfig {
    const val DEBUG = true
}
