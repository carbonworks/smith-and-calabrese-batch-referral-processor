# March 2026 — Prompt Logs

## 2026-03-02

### Move application code into app/ subdirectory + UI polish fixes

**Session log:**

1. **User**: Implement plan to move application code into `app/` subdirectory
   - Converted to Gradle multi-module layout: `git mv src app/src`, `git mv build.gradle.kts app/build.gradle.kts`
   - Updated `settings.gradle.kts` with `include("app")`
   - Updated path references in CLAUDE.md, work-packages.md, architectural-review.md
   - Added `vx_recycle_bin/` to .gitignore
   - Deleted all VNote metadata files (`vx.json`, `vx_notebook/`, `vx_recycle_bin/`)
   - Verified `./gradlew :app:test` — all 98 tests pass

2. **User**: "let's clean up and commit as necessary"
   - Committed as `efe3f1c`: Move application code into app/ subdirectory

3. **User**: "update the memory file with the new app/ paths"
   - Added Project Layout section to MEMORY.md with app/ paths and `:app:` task prefix
   - Updated code change policy reference from `src/` to `app/src/`

4. **User**: "is anything remaining on the backlog?"
   - Checked work-packages.md — all 22 WPs are `done`, backlog is clear

5. **User**: "the help and settings icons are still gray instead of green"
   - Launched worktree agent to change Settings/Help icon tints from SoftGray to BrandGreen in MainScreen.kt
   - Agent couldn't commit; applied changes directly, committed as `d508c73`

6. **User**: "also symbol-green.svg is hanging out uncommitted. let's remove it"
   - Deleted `symbol-green.svg` from repo root (untracked file, just removed)
   - Also cleaned up a stray `vx.json` in `app/src/main/resources/`

7. **User**: Four UI issues: (1) remove icon misaligned, (2) need more section spacing, (3) drag-drop hover square corners, (4) mask toggle doesn't unmask
   - Launched worktree agent for all four fixes
   - Issue 1: Replaced IconButton with plain clickable Icon (later reverted — see #9)
   - Issue 2: Bumped inter-section spacers from 16dp to 24dp across MainScreen, ProcessingScreen, ResultsScreen
   - Issue 3: Added `.clip(RoundedCornerShape(12.dp))` to drop zone Box
   - Issue 4: Threaded `isMasked` Compose state through ReferralCard → child composables for recomposition
   - Committed as `657a3e9`

8. **User**: "this sounds like a backwards fix. touch targets are important" + "lets add some basic navigation animation"
   - Key decision: user correctly identified that removing IconButton touch targets was wrong
   - Launched worktree agent for both fixes
   - Restored IconButton (32dp touch target), reduced row padding from 4dp to 2dp for alignment
   - Added `Crossfade(animationSpec = tween(300))` around screen router in Main.kt
   - Committed as `8f4007f`

9. **User**: "let's clean up old worktree branches"
   - Removed 24 worktree directories and 28 branches
   - Clean state: one worktree (main), one branch

10. **User**: "let's log and commit" (previous session prompt log committed as `6d44b42`)

---

## 2026-03-02 (session 2)

### Fix PHI eye toggle not unmasking displayed data

**Session log:**

1. **User**: "is there any work left in the backlog?"
   - Checked work-packages.md — all 22 WPs are `done`, backlog clear

2. **User**: Reported unmask toggle doesn't work, shared log output showing recomposition fires but data stays masked
   - Explored PhiMask.kt, ResultsScreen.kt, PhiPreferences.kt
   - Root cause: `isMasked` Compose state was threaded to every child composable but never used — all display calls went through `PhiMask.maskDisplay()` which reads a plain Boolean invisible to Compose's recomposition system
   - Launched worktree agent for the fix; agent identified all changes but was blocked on edit permissions
   - Applied 5 edits directly: changed `PhiMask.maskDisplay(value)` → `if (isMasked) PhiMask.maskDisplay(value) else value` in MetadataRow, ServiceItem (×3), FooterSection, error section, warnings section
   - All tests pass; committed as `c8ba114`

3. **User**: "update your settings to never use git commit as part of a compound command"
   - Added rule to memory MEMORY.md

4. **User**: "this should also be in the project claude config and other related processes"
   - Added to CLAUDE.md Git Practices: "Never chain git commands — run git add, git commit, git push, etc. as separate, standalone Bash invocations"
   - Updated memory to match broader scope (all git commands, not just commit)

5. **User**: "the app doesn't honor the new setting in settings screen — add a backlog item"
   - Added WP-23 (B12) to work-packages.md: "Show Extracted Data by Default" setting not honored

6. **User**: "can we run another wave?"
   - Launched worktree agent for WP-23
   - Agent identified fix but was blocked on Bash permissions for git

7. **User**: "compound commands with cd and git require approval — update the project to avoid them"
   - Added `cd && git` prohibition to CLAUDE.md and memory — use `git -C <path>` instead
   - Reviewed worktree diff using `git -C`, applied 3 changes to main:
     - PhiMask.kt: added `refreshFromPreferences()` method
     - ResultsScreen.kt: calls `refreshFromPreferences()` in `remember` block before initializing `isMasked`
     - PhiMaskTest.kt: new test verifying refresh syncs stale `maskingEnabled` with persisted preference
   - All tests pass, WP-23 marked done

8. **User**: "2 new backlog items: copy/paste for results values, origami bird app icon"
   - Added WP-24 (E8) and WP-25 (E9) to work-packages.md
   - Launched both agents in parallel

9. **User**: "let's run them"
   - WP-24 agent completed: added `CopyableValue` composable with click-to-copy, color flash, "Copied" indicator
   - WP-25 agent completed: added icon generation Gradle task, jpackage config, Main.kt icon loading, Python generator script
   - Merged WP-24 (conflict resolution: took CopyableValue over plain Text)
   - Merged WP-25 (clean merge)
   - Build failure: Gradle icon task used `java.awt` classes shadowed by Kotlin DSL `java` plugin
   - Fix: added explicit imports, hit IR lowering error from `data class` in task lambda
   - Resolution: removed Gradle icon task entirely, kept Python script as icon generator

10. **User**: "click to copy isn't normal for Windows — I expected drag to highlight"
    - User correctly identified web/mobile pattern vs desktop convention
    - Added WP-26 (E10) to replace CopyableValue with native SelectionContainer

11. **User**: "app title should be 'PDF Referral Parser - Carbon Works'"
    - Added WP-27 (E11) to backlog

12. **User**: "progress bar has purple — only green and orange in the app"
    - Added WP-28 (B13) to backlog
    - Launched all three (WP-26, WP-27, WP-28) in parallel

13. Wave results:
    - WP-27 merged with conflict resolution (kept icon param from HEAD, took title from worktree)
    - WP-28 merged cleanly (added trackColor = LightGray to LinearProgressIndicator)
    - WP-26 merged with 5 conflict resolutions: replaced all CopyableValue with plain Text inside SelectionContainer, removed dead CopyableValue/copyToClipboard code, cleaned up 10 unused imports
    - All tests pass, WP-26/27/28 marked done

---

## 2026-03-03

### Add column reorder UX work packages to backlog

**Session log:**

1. **User**: (continuation session) Reported `cw-emblem.svg` not found error, requested backlog items, corrected direct code edits
   - Previous session: reverted direct edits, launched WP-29/30/31 as proper worktree agents, merged all three
   - Ran WP-32 (remove watermark + title suffix), WP-33 (rebrand to authorization terminology)
   - Researched and implemented configurable spreadsheet export (WP-34 feature flags, WP-35 data model, WP-36 SpreadsheetWriter refactor, WP-37 Settings UI)
   - Added WP-38 (fix title/subtitle text)
   - Researched column reorder UX improvements (individual options: jump buttons, inline spacer insertion, drag-and-drop, context menus)

2. **User**: "is this meant to be 3 different options? tier 1, tier 2, or tier 3?"
   - Clarified: tiers are priority/effort levels, not mutually exclusive — user can mix and match individual items

3. **User**: "let's run another research agent that builds a unified experience definition"
   - Launched research agent that surveyed AG Grid, DataGrip, DBeaver, Power BI, Tableau, macOS Finder, NNGroup, PatternFly, Salesforce, WCAG 2.2
   - Key finding: drag-and-drop is an accelerator, not primary — buttons are universal baseline
   - Recommended design: drag-enhanced checklist with overflow menus
   - Layout: drag handle | checkbox | label | up/down arrows | three-dot overflow menu
   - Overflow menu: Move to Top, Move to Bottom, Insert Spacer Above/Below, Remove (spacers)
   - Deliberately excluded: right-click context menus (low discoverability), inline "+" buttons between rows (visual noise), full keyboard nav (deferred)
   - One new dependency: `sh.calvin.reorderable:reorderable:3.0.0`

4. **User**: "yes" (add to backlog)
   - Added WP-39 (F4): Drag-and-Drop Column Reordering — add reorderable dependency, migrate to ReorderableLazyColumn, drag handles, drag feedback, auto-scroll
   - Added WP-40 (F5): Overflow Menu and Spacer Insertion UX — per-row three-dot menu, jump moves, targeted spacer insertion, visual differentiation
   - WP-39 depends on WP-37 (done), WP-40 depends on WP-39

5. **User**: "log, commit, and push. then start the wave."
   - Committed prompt log + work-packages.md, pushed
   - Launched WP-38 (title/subtitle fix) and WP-39 (drag-and-drop) in parallel
   - WP-38 merged: changed title to "Batch Authorization Processor", spelled out "Maryland Disability Determination Services"
   - WP-39 merged: added `sh.calvin.reorderable:reorderable:3.0.0`, migrated to ReorderableLazyColumn, drag handles with elevation animation
   - Launched WP-40 (overflow menus + spacer insertion); merged: per-row three-dot menus, inline close button for spacers

6. **User**: Five quick-fix backlog items
   - Added WP-41 (remove duplicate spacer close button), WP-42 (reset button), WP-43 (scrollbar), WP-44 (remove insert column button), WP-45 (save results UX)
   - Ran all five across two waves, resolved merge conflicts in SettingsScreen.kt
   - All merged and marked done

7. **User**: Four new backlog items + one bug
   - Added WP-46 (move export settings to own screen), WP-47 (reset confirmation dialog), WP-48 (expand services checkbox), WP-49 (export settings on results screen)
   - Added WP-50 (fix Help back nav from results)

---

## 2026-03-03 (session 2)

### Implement WP-46, WP-50, add navigation migration backlog item

**Session log:**

1. **User**: (continuation session) Resumed from context overflow
   - Working directory was stuck in deeply nested worktree path; reset to repo root
   - Cleaned up stale worktree directories

2. **User**: "does the current engineering leverage compose multiplatform navigation systems or basic compose mechanics?"
   - Confirmed: basic Compose mechanics — hand-rolled `enum class Screen` + `mutableStateOf` + `when` block with `Crossfade` animation, no NavHost/NavController/back stack

3. **User**: "please add an item to the backlog to migrate to official navigation mechanics"
   - Added WP-51 (R3): Migrate to Compose Multiplatform Navigation — replace enum/when with `NavHost`/`NavController` from `navigation-compose`, built-in back stack, AnimatedNavHost transitions
   - Depends on WP-46, WP-49, WP-50

4. Launched WP-46 + WP-50 in parallel as worktree agents
   - WP-50 merged (fast-forward): added `previousScreen` state variable, updated all Help/Settings `onBack` callbacks to use it
   - WP-46 merged (conflict resolution in Main.kt — kept `previousScreen` from WP-50, added `onNavigateToExportSettings` + `EXPORT_SETTINGS` screen from WP-46): extracted entire export column UI into new `ExportSettingsScreen.kt`, SettingsScreen now shows clickable navigation row with chevron
   - All tests pass
   - WP-47, WP-48, WP-49 now unblocked (status: ready)

5. Launched WP-47 (reset confirmation dialog) + WP-49 (export settings button on results) in parallel
   - WP-49 merged (fast-forward): added feature-flagged "Export Settings" button to Results action bar, updated ExportSettingsScreen onBack to use `previousScreen` for dynamic return
   - WP-47 merged (auto): added `AlertDialog` with "reset" text input confirmation to Reset button on ExportSettingsScreen
   - All tests pass

6. **User**: "yes and follow it immediately by wp-51"
   - Launched WP-48 (expand services to individual rows)
   - WP-48 merged (auto): added `expandServices` boolean to `ExportColumnConfig`, checkbox on ExportSettingsScreen, SpreadsheetWriter row expansion logic, 6 new tests
   - Launched WP-51 (navigation migration)
   - WP-51 merged (conflict resolution — added missing `onNavigateToExportSettings` from WP-49, fixed stray brace): replaced `enum class Screen` + `Crossfade` + `when` with `NavHost`/`NavController`/`composable()` routes, removed `previousScreen` tracking, added `navigation-compose-desktop:2.8.0-alpha10` dependency, preserved fadeIn/fadeOut transitions
   - All tests pass, all 51 WPs done
   - Backlog clear

---

## 2026-03-03 (session 3)

### WP-52 through WP-62: E2E tests, nav animation research, UI polish wave

**Session log:**

1. **User**: "please add to the backlog defining a set of manual end to end tests"
   - Added WP-52 (T1): Manual E2E Test Script — 14-checkbox quick-pass smoke test + 9 full test cases

2. **User**: "add a research task for navigation animation strategy, kick off both"
   - Added WP-53 (R4): Navigation Animation Strategy research
   - Launched WP-52 + WP-53 in parallel
   - WP-52 merged: created `docs/testing/manual-e2e-tests.md` with P0/P1 flagged test cases
   - WP-53 merged: created `docs/research/navigation-animation-strategy.md` (540 lines, per-route animation map)

3. **User**: Five backlog items for UI polish
   - WP-54 (B15): checkbox alignment — negative X offset `(-12).dp`
   - WP-55 (B16): privacy toggle gutter — 16dp Spacer between description and Switch
   - WP-56 (E26): system save dialog — `java.awt.FileDialog` save mode, `Preferences` for directory memory
   - WP-57 (E27): save feedback reposition — `AnimatedVisibility` with `fadeIn + expandVertically`
   - WP-58 (R5): filename terminology — changed `patient-referrals-` to `authorizations-`

4. **User**: "run 2 at a time until they are all complete"
   - Wave 1: WP-55 + WP-56 — both merged cleanly
   - Wave 2: WP-54 + WP-58 — both merged (nested worktree paths on Windows)
   - Wave 3: WP-57 — merged cleanly
   - All five marked done, committed status update

5. **User**: Four more backlog items
   - WP-59 (B17): drag-and-drop fix — walks AWT component tree, attaches DropTargetAdapter to all components
   - WP-60 (E28): move expand services checkbox above preset buttons
   - WP-61 (B18): subtitle spacing on Main and Results screens
   - WP-62 (E29): Start Over button to far left of action bar

6. **User**: "run waves until the backlog is empty, keep up with logs and commits"
   - Wave 1: WP-59 + WP-60 — both merged cleanly, tests pass
   - Wave 2: WP-61 + WP-62 — both merged cleanly, tests pass
   - All four marked done
   - Backlog clear, all 62 WPs done

7. **User**: Reported city/state/zip appearing as orphaned unlabeled row below Appointment in data preview
   - Investigated: `parseClaimantCell()` assumed single-line text but real PDFs have multi-line claimant cells (name / street / city,ST zip / phone)
   - Street address extraction failed because word-count heuristic didn't match multi-line collapsed format
   - Added WP-63 (B19): rewrite parser to be line-aware + display fallback
   - Added WP-64 (B20): add completeness warnings for missing expected fields
   - Launched both in parallel
   - WP-63 merged (fast-forward): rewrote `parseClaimantCell()` with `parseClaimantCellMultiLine()` for line-aware parsing, single-line fallback preserved, display fix for orphaned city/state/zip, 11 new tests
   - WP-64 merged (conflict in test file, resolved by keeping both test sets): added `EXPECTED_FIELDS` list, `generateMissingFieldWarnings()` companion method, per-card orange "Missing fields" banner in ResultsScreen, 7 new tests
   - All tests pass, both marked done
   - Backlog clear, all 64 WPs done

8. **User**: Reported street address warning still showing despite WP-63 fix
   - Launched research agent to trace data flow from PDF through Tabula to parser
   - Root cause: Tabula's `RectangularTextContainer.text` joins all cell text with spaces, never newlines — the WP-63 multi-line parser never fires with real PDFs
   - Added WP-65 (B21): fix `[completeness]` tag word-wrapping in warning banner
   - Added WP-66 (B22): preserve line breaks in TableExtractor by reconstructing from Y-coordinates + clean up dead single-line parser code
   - Launched both in parallel
   - WP-65 merged (fast-forward): widened stage tag column from 64dp to 100dp
   - WP-66 merged (auto): added `extractCellText()` to TableExtractor with Y-coordinate line grouping, removed `parseClaimantCellSingleLine()`, updated all tests to use newline-separated cell text
   - All tests pass, both marked done
   - Backlog clear, all 66 WPs done

9. **User**: Confirmed address extraction fixed. Requested new features: "Report an Issue" log save, logging audit, PHI sanitization audit, file logging with rotation, rename Save button.
   - Added WP-67 (B23): drag-and-drop cursor showing "move" instead of "copy"
   - Added WP-68 (R6): audit logging coverage for triage
   - Added WP-69 (S1): audit logging for PHI sanitization
   - Added WP-70 (F6): file-based logging with rotation (depends WP-68, WP-69)
   - Added WP-71 (F7): report an issue — save logs to file (depends WP-70)
   - Added WP-72 (E30): rename Save button to "Save as Spreadsheet"
   - Wave 1a: WP-67 + WP-72 — both merged cleanly
     - WP-67: added `dragOver` handler with `acceptDrag(ACTION_COPY)`, changed DropTarget to `ACTION_COPY_OR_MOVE`
     - WP-72: renamed button text from "Save" to "Save as Spreadsheet"
   - Wave 1b: WP-68 + WP-69 — both merged cleanly
     - WP-68: added structured `[Component]` logging across 10 files (extraction, UI, output, navigation, settings)
     - WP-69: sanitized 15 log statements — removed filenames, file paths, error messages, replaced with counts/booleans/class names
   - Wave 2: WP-70 — merged with conflict in Main.kt (resolved: took block body + LoggingSetup.initialize(), dropped redundant manual println lines)
     - Created `logging/` package: `LoggingSetup`, `RotatingFileOutputStream`, `TeeOutputStream`
     - Platform-specific log dirs, 5MB rotation, 2 backups, stdout/stderr tee, 13 tests
   - Wave 3: WP-71 — merged (fast-forward)
     - Added "Save Log File" button to HelpScreen with FileDialog save-as, concatenates rotated logs oldest-first
     - Passed window ref to HelpScreen in navigation graph
   - All tests pass, all six marked done
   - Backlog clear, all 72 WPs done

10. **User**: Seven UI polish items: rename buttons/screens, gear icon, essential-only behavior, spacing, per-item unmask
    - Added WP-73 (E31): rename "Save as Spreadsheet" to "Export"
    - Added WP-74 (E32): replace Export Settings text button with gear icon
    - Added WP-75 (B24): essential only should uncheck non-essential columns, not remove them
    - Added WP-76 (E33): rename "Export Columns" screen title to "Export Settings"
    - Added WP-77 (E34): shorten Settings nav link to "Export"
    - Added WP-78 (E35): more spacing between back button and list in Export Settings
    - Added WP-79 (F8): per-item unmask toggle in data preview cards
    - Wave 1: WP-77 + WP-75 — both merged cleanly
    - Wave 2: WP-73 + WP-76 — both merged cleanly
    - Wave 3: WP-74 + WP-78 — both merged cleanly
      - WP-74: replaced CwSecondaryButton with IconButton using Icons.Outlined.Settings, BrandGreen tint
      - WP-75: changed essential-only to map existing columns with enabled flag instead of rebuilding list
    - Wave 4: WP-79 — merged cleanly
      - Added per-card mutableStateMapOf overrides, eye icon between filename and Open PDF link, global toggle clears overrides
    - All tests pass, all seven marked done
    - Backlog clear, all 79 WPs done

11. **User**: Three more UI items: title padding, log save open folder link, spacing above save log button
    - Added WP-80 (B25): right padding on title/description text on main screen
    - Added WP-81 (E36): "Open folder" link in log save feedback on Help screen
    - Added WP-82 (E37): more spacing above Save Log File button
    - WP-80 + WP-81 launched in parallel, both merged cleanly
    - WP-82 launched after WP-81 (same file), merged cleanly
    - All tests pass, all three marked done
    - Backlog clear, all 82 WPs done

12. **User**: Update support log copy with PHI masking note, asked about log capture mechanics
    - Added WP-83 (E38): update instructional text above Save Log File button
    - Explained log pipeline: LoggingSetup → TeeOutputStream → RotatingFileOutputStream, 5MB rotation, PHI sanitized
    - WP-83 merged (fast-forward), marked done
    - Backlog clear, all 83 WPs done

13. **User**: Two more backlog items, then package build
    - Added WP-84 (B26): scrollbar on selected files list on main screen
    - Added WP-85 (B27): fix per-item unmask icon vertical alignment + change to BrandGreen
    - Both launched in parallel, both merged cleanly
    - All tests pass, both marked done
    - Backlog clear, all 85 WPs done
    - Attempted package build next

14. **User**: Research installer branding, then run installer improvements
    - Launched explore agent for installer branding audit
    - Key findings: icon.ico is only 16x16 (needs multi-res), no EULA, generic description
    - Added WP-86 (E39): regenerate multi-resolution icon.ico (16/32/48/64/256px)
    - Added WP-87 (E40): add license/EULA to installer (blocked on license research)
    - Added WP-88 (E41): update installer description to reflect brand
    - Launched license research agent in parallel with WP-86 + WP-88
    - WP-88 merged: description updated to "Turns referral PDFs into organized spreadsheets, quickly and accurately."
    - WP-86 completed manually (agent couldn't run Python): regenerated icon.ico with 5 resolution layers using Pillow
    - License research returned: proprietary EULA recommended with PHI clauses, warranty disclaimer, liability limitation
    - WP-87 merged: created LICENSE.txt with proprietary EULA (PHI clauses, warranty disclaimer, liability limitation, open-source attribution), wired into build.gradle.kts windows{} block
    - All tests pass, WP-86/87/88 all marked done

15. **User**: Three installer issues — license doesn't match contract, no branding, red disk default images
    - Added WP-89 (B28): rewrite LICENSE.txt to mirror contract Section 10 IP terms (perpetual, irrevocable, royalty-free license with modification rights)
    - Added WP-90 (E42): brand MSI installer with custom banner/dialog BMP images
    - WP-89 merged: rewrote LICENSE to match contract — perpetual/irrevocable/royalty-free, removed MAY NOT restrictions, added plain-language summary, version bumped to 2.0
    - WP-90 implemented directly: generated branded banner.bmp (493x58) and dialog.bmp (493x312) with origami bird + Carbon Works colors, overrode jpackage main.wxs with WixVariable declarations, added Gradle copy task for resource injection
    - Research: decompiled jpackage jmod to find resource override mechanism, traced `--resource-dir` through Compose plugin bytecode, confirmed WiX UI Extension provides default red images via WixUIBannerBmp/WixUIDialogBmp variables
    - WP-90 attempted: generated branded BMPs, overrode main.wxs with WixVariable declarations, tried Copy task then doFirst with absolute paths — build succeeds but branding still not appearing in installer. Root cause unresolved; likely jpackage ignoring custom main.wxs or WiX variable override mechanism differs from documentation. WP-90 reverted to ready.
    - All tests pass, WP-89 done, WP-90 open
    - 89 of 90 WPs done

16. **User**: Demo walkthrough document, then UI fixes
    - Created `docs/demo/walkthrough.md` — 5-7 minute onboarding tour with description-then-bullets format
    - Added WP-91 (B29): fix vertical alignment of per-card header row (eye icon, filename, Open PDF link)
    - Added WP-92 (B30): fix per-card mask toggle interaction with global toggle
    - WP-91 merged: removed redundant `.align(CenterVertically)` from IconButton, let parent Row handle alignment
    - WP-92 merged: global toggle now explicitly sets every card's state in `perCardMaskOverrides` map instead of just clearing
    - All tests pass, both marked done
    - Added WP-93 (B31): per-card toggles broken when global is unmasked (WP-92's repopulation loop interfered with Compose snapshot recomposition)
    - WP-93 merged: reverted to `perCardMaskOverrides.clear()` only — fallback to `isMasked` works correctly with Compose's state observation
    - 92 of 93 WPs done (WP-90 installer branding still open)

---

## 2026-03-05

### Update architectural review (revision 2)

**Session log:**

1. **User**: "let's do an architectural review based on compose and kotlin multiplatform best practices"
   - Launched thorough Explore agent to review all 29 source files (6,451 lines) against Compose/Kotlin best practices
   - Assessed 10 areas: state management, architecture/separation, navigation, side effects, recomposition safety, coroutines/threading, error handling, code organization, Compose Desktop specifics, anti-patterns
   - Found 5 new deviations (D6a, D11-D15), marked D3 and D8 as worsened
   - Key new findings: ResultsScreen exceeds 1,051 lines, saveToXlsx() I/O on UI thread, UIManager.setLookAndFeel() called on every file picker open, side effect in remember initializer
   - Updated `docs/architecture/architectural-review.md` (revision 2) with full findings, deviation tracker, and prioritized recommendations

2. **User**: "backlog: the logs are dumping data from pages that are not 2 and 3"
   - Added WP-94 (S2): Restrict Logging to Pages 2–3 to Prevent PHI Leakage
   - Launched worktree agent; merged (fast-forward)
   - Added `TARGET_PAGES = setOf(2, 3)` constant, gated `dumpPageTexts()` and `dumpPageTextsDetailed()` to skip non-target page content
   - PdfTextExtractor and TableExtractor were clean — only metadata logged
   - All tests pass, WP-94 marked done

3. **User**: "run a research agent to determine what data to keep in the logs and what data to not log"
   - Launched thorough Explore agent to audit all println statements, classify every field as PHI/operational/structural
   - Critical finding: `dumpPageTextsDetailed()` uses `PhiMask.maskDisplay()` which respects the UI toggle — when masking is disabled, raw PHI is logged
   - Recommended formula: log structural counts freely, always-mask file names and dump content, never log raw field values

4. **User**: "Don't we need to validate known vs unknown data as well?"
   - User identified that blindly logging page 2-3 content is still risky if PDF format changes
   - Agreed: known non-PHI fields → log freely, known PHI fields → log masked, unknown content → do not log

5. **User**: "let's create backlog to remove any logging mechanics around logging PDF content"
   - User decided simplest approach: remove all PDF content logging entirely rather than complex masking
   - Added WP-95 (S3): Remove All PDF Content Logging from Extraction Pipeline
   - Launched worktree agent; merged (fast-forward, -177 lines)
   - Deleted `dumpPageTexts()`, `dumpPageTextsDetailed()`, `TARGET_PAGES`, `knownLabels`
   - Stripped file names from all println in extraction pipeline (11 statements)
   - Replaced all `e.message` with `e::class.simpleName` in catch blocks
   - Removed dump call site in ProcessingScreen.kt and dump test in FieldParserTest.kt
   - All tests pass, WP-95 marked done

6. **User**: Created release candidate tag, built installer, discussed installer suites and licensing
   - Added `docs/protocols/release-tagging.md` with major.minor.hotfix versioning, RC workflow
   - Tagged `v1.0.0-rc1` at `7c57ec3`, pushed tag
   - Built MSI installer successfully
   - Discussed WiX alternatives (Inno Setup as best alternative for branding)
   - Discussed licensing: reviewed current contract-mirrored EULA, considered Apache 2.0
   - User proposed dual licensing: contract governs client, Apache 2.0 governs software
   - Researched best practices: no HIPAA requirement for installer license screen, industry trend is to skip it, Apache 2.0 doesn't require click-through
   - Created WP-96 through WP-100 (licensing wave):
     - WP-96: Replaced EULA with standard Apache 2.0 license
     - WP-97: Added NOTICE.txt with third-party attributions (9 dependencies)
     - WP-98: Removed click-through license dialog from MSI installer
     - WP-99: Added Licensing section to Help screen (fixed agent's stale EULA references)
     - WP-100: Added Data Privacy and Data Validation sections to Help screen
   - All five merged (one conflict in HelpScreen.kt resolved: both sections kept, ordering corrected)
   - All tests pass, WP-96–100 marked done

7. **User**: "backlog: the ripple on the open-source components expand control is clipped by the parent"
   - Added WP-101 (B32): Fix clipped ripple on open-source components expand control
   - Launched worktree agent; agent added `.clip(RoundedCornerShape(8.dp))` — wrong approach (masked symptom)
   - User: "WP-101 is done wrong. instead of changing the ripple shape, the configuration for the component should be that it isn't clipped by its parent."
   - Fixed properly: moved clickable Row and expanded content outside the padded Column so ripple fills full card width, clipped by Card's own RoundedCornerShape(12.dp)
   - Removed unused clip and RoundedCornerShape imports
   - All tests pass, WP-101 marked done

8. **User**: "run a background agent to analyze the help & support content exhaustively for needed updates"
   - Launched Help screen audit agent (opus); found 24 issues (2 critical, 5 major, 12 minor, 5 nit)
   - Critical: "Save to XLSX" button doesn't exist (it's "Export"), log PHI masking claim inaccurate
   - Major: "preview table" (uses cards), "warnings panel" (doesn't exist), Export Settings/per-card masking/Settings undocumented

9. **User**: "yes. please backlog and wave through the related work packages."
   - Created WP-102 (B33): Fix 7 inaccurate text items in Help screen
   - Created WP-103 (E43): Add 9 missing feature documentation sections
   - Created WP-104 (B34): 3 minor wording and consistency fixes
   - Wave 1: WP-102 merged (auto)
   - Wave 2: WP-103 merged (conflict: both sides fixed "Export" — kept WP-102's wording + WP-103's new Step 5)
   - Wave 3: WP-104 merged (two conflicts: combined processing details with button wording, kept new bullets + updated triple-click wording)
   - All tests pass, WP-102–104 marked done
   - 103 of 104 WPs done (WP-90 installer branding still open)

---

## 2026-03-07

### RC2 build, log analysis, jlink module fix

**Session log:**

1. Tagged `v1.0.0-rc2` at `f1006e6`, built MSI installer, pushed tag

2. **User**: Provided runtime log from tester's machine (`__inbox/batch-auth-processor-log-2026-03-07.txt`)
   - `NoClassDefFoundError: java/sql/Date` at `SpreadsheetWriter.kt:196` — export crashes in packaged installer
   - Root cause: jlink runtime image missing `java.sql` module; `java.sql.Date.valueOf()` used for date cell formatting
   - Only manifests in packaged MSI, not during `./gradlew run` (full JDK on classpath in dev)
   - Also noted: ExportPreferences saves on every checkbox toggle (80+ saves in session) — separate concern

3. **User**: "How do I find and resolve this and similar runtime dependency issues?"
   - Explained jlink static analysis limitations, recommended `jdeps --print-module-deps`, smoke-testing installer builds, explicit module declarations
   - Ran `jdeps` against app JAR: requires `java.base, java.desktop, java.prefs, java.sql`
   - Checked runtime image `release` file: includes `java.base, java.datatransfer, java.xml, java.prefs, java.desktop, java.logging, jdk.crypto.ec`
   - Only `java.sql` missing — no other module gaps

4. **User**: "Let's remove the dependency via work package"
   - Added WP-105 (B35): Replace `java.sql.Date.valueOf(parsedDate)` with `parsedDate` directly — POI 5.3.0 natively accepts `LocalDate`
   - Launched worktree agent; merged (fast-forward, 1 line changed)
   - No other `java.sql` references in codebase
   - All tests pass, WP-105 marked done
   - 104 of 105 WPs done (WP-90 installer branding still open)

5. Built MSI installer, tagged `v1.0.0-rc3` — user corrected: v1.0.0 was already released to client, so this is a hotfix
   - Deleted `v1.0.0-rc3` tag (local + remote)
   - Bumped `packageVersion` to `1.0.1` in build.gradle.kts
   - Tagged `v1.0.1-rc1` at `d083622`, rebuilt installer as `PDF Authorization Processor-1.0.1.msi`
   - Confirmed MSI major upgrade behavior: silently replaces previous version, user data (Registry prefs, AppData logs) unaffected

6. **User**: "Let's add the jlink scan to the release process. Also add update processes for the FOSS licensing list."
   - Updated `docs/protocols/release-tagging.md` with Pre-Release Checklist:
     1. jlink module scan — `jdeps --print-module-deps` vs runtime image `MODULES`
     2. FOSS licensing review — diff `./gradlew dependencies` against NOTICE.txt and HelpScreen.kt
     3. Version number verification

---
