# Prompt Log — February 2026

## 2026-02-23

### Establish prompt logging for the project

**Session log:**

1. **User**: "hi. please see where we are with this project. also, make sure prompt logging is in place. give me an update on both."
   - Explored full project status: scaffolding complete, extraction engine/XLSX/UI not started
   - Confirmed build compiles and runs, all dependencies declared
   - Checked for prompt logging — found only Claude Code's automatic JSONL logs, no structured project logging

2. **User**: "let's get some local prompt logging established. Please check ~/projects/CarbonWorksIntranet for how prompt logging is handled there."
   - Reviewed CarbonWorksIntranet prompt logging protocol and log format
   - Created `docs/protocols/prompt-logging.md` adapted from Intranet version
   - Created `archive/prompts/2026/02.md` with this session's log
   - Updated CLAUDE.md with prompt logging instructions and project structure entry

3. **User**: "Please look at the ~/projects/StreetSoccer project for how it runs 'waves' of implementation work. Let's bake the wave functionality into this project."
   - Reviewed StreetSoccer `work-packages.md` and CLAUDE.md Parallel Agent Workflow section
   - Created `work-packages.md` with 7 work packages (WP-0 through WP-6) restructured from existing backlog
   - WP-0: PDF Text Extraction Core, WP-1: Field Parsing, WP-2: Table Extraction, WP-3: XLSX Output, WP-4: Desktop UI, WP-5: OCR Fallback, WP-6: Packaging
   - Defined 5 waves based on dependency graph: Wave 1 (WP-0, WP-2), Wave 2 (WP-1, WP-5), Wave 3 (WP-3), Wave 4 (WP-4), Wave 5 (WP-6)
   - Added full "Parallel Agent Workflow" section to CLAUDE.md with agent prompt template, launch instructions, conflict resolution
   - Key decision: adapted StreetSoccer's Owns/Reads/Touches/Depends model to this project's extraction pipeline

### Wave 1: Launch WP-0 and WP-2 in parallel

**Session log:**

1. **User**: "Implement the following plan: Wave 1: Launch WP-0 and WP-2 in Parallel"
   - Pre-launch: verified `./gradlew build` clean, git working tree clean
   - Read all reference files: `work-packages.md`, `build.gradle.kts`, Python scripts, `Main.kt`
   - Launched two parallel worktree-isolated agents (WP-0 and WP-2)
   - WP-0 agent: created `ExtractionResult.kt`, `PdfTextExtractor.kt`, `PdfTextExtractorTest.kt` (5 tests), added test deps to `build.gradle.kts`
   - WP-2 agent: created `TableExtractor.kt`, `TableExtractorTest.kt` (3 tests), added test deps to `build.gradle.kts`
   - Both agents completed successfully with all tests passing
   - Merged WP-0 first — hit `vx.json` duplicate resource error from PDFBox/Tabula transitive dependency overlap
   - Fixed with `DuplicatesStrategy.EXCLUDE` on Copy and Jar task types
   - Merged WP-2 second — resolved expected trivial conflict in `build.gradle.kts` (test deps already present)
   - All 8 tests pass, `./gradlew build` succeeds
   - Updated `work-packages.md`: WP-0 and WP-2 → `done`, WP-1 and WP-5 → `ready`
   - Key finding: Tabula-java 1.0.5 works with PDFBox 3.0.4 despite being built against 2.x

### Wave 2: Launch WP-1 (Field Parsing) and remove WP-5

**Session log:**

1. **User**: "Implement the following plan: Wave 2: Launch WP-1 (Field Parsing)"
   - Pre-launch housekeeping: removed WP-5 (OCR Fallback) entirely — referral PDFs are native text, not scanned
   - Removed Tess4J dependency from `build.gradle.kts`
   - Updated dependency graph and wave list in `work-packages.md` to reflect WP-5 removal
   - Verified `./gradlew build` clean, committed cleanup
   - Launched WP-1 as worktree-isolated agent with comprehensive prompt including all Python extraction logic to port
   - WP-1 agent created 3 files: `ReferralFields.kt` (data model with ParsedField/Confidence/ServiceLine), `FieldParser.kt` (553 lines porting all Python regex logic), `FieldParserTest.kt` (11 tests)
   - Merged WP-1 into main — clean fast-forward, no conflicts
   - All 19 tests pass (8 existing + 11 new), `./gradlew build` succeeds
   - Updated `work-packages.md`: WP-1 → `done`, WP-3 → `ready`
   - Key design: TextBlocks concatenated per page with space separators for regex matching; priority merge header > table > invoice > fallback

### Wave 3: Launch WP-3 (XLSX Output)

**Session log:**

1. **User**: "Implement the following plan: Wave 3: Launch WP-3 (XLSX Output)"
   - Verified codebase clean from Wave 2, all dependencies met (WP-0, WP-1, WP-2 done)
   - Read `work-packages.md`, `ReferralFields.kt`, `build.gradle.kts`, `build-plan.md` D2/D3 specs
   - Launched WP-3 as single worktree-isolated agent
   - WP-3 agent created 2 files: `SpreadsheetWriter.kt` (146 lines, Apache POI XSSFWorkbook), `SpreadsheetWriterTest.kt` (262 lines, 8 tests)
   - SpreadsheetWriter: 23 columns mirroring ReferralFields order + Low Confidence Flag, bold frozen header, comma-joined CPT codes for Services, all string cells for Google Sheets compatibility
   - Merged WP-3 into main — clean fast-forward, no conflicts (new `output/` package, no shared files)
   - All tests pass (19 existing + 8 new = 27 total), `./gradlew build` succeeds
   - Updated `work-packages.md`: WP-3 → `done`, WP-4 → `ready`
   - Newly unblocked: WP-4 (Desktop UI). Still blocked: WP-6 (needs WP-4)

### Wave 4: Launch WP-4 (Desktop UI)

**Session log:**

1. **User**: "Implement the following plan: Wave 4: Launch WP-4 (Desktop UI)"
   - Session crashed mid-execution; user restarted and re-executed the plan
   - Verified codebase clean from Wave 3, all dependencies met (WP-0, WP-1, WP-2, WP-3 done)
   - Read all existing source files: Main.kt, ReferralFields.kt, SpreadsheetWriter.kt, PdfTextExtractor.kt, ExtractionResult.kt, TableExtractor.kt, FieldParser.kt, Theme.kt, brand guidelines
   - Launched WP-4 as single worktree-isolated agent with comprehensive prompt covering all design decisions
   - WP-4 agent created 10 files (1,455 lines total):
     - 6 reusable components: CwButton.kt, CwCard.kt, SectionHeader.kt, FilePathText.kt, ConfidenceBadge.kt, StatusIcon.kt
     - 3 screens: MainScreen.kt (377 lines — file picker, drag-and-drop, file list), ProcessingScreen.kt (228 lines — per-file progress, pipeline wiring), ResultsScreen.kt (467 lines — data preview table, error summary, save-to-XLSX)
     - Modified Main.kt: Screen enum navigation, state hoisting, window size 1100x700
   - Merged WP-4 into main — clean fast-forward, no conflicts
   - All 27 tests pass, `./gradlew build` succeeds
   - Updated `work-packages.md`: WP-4 → `done`, WP-6 → `ready`
   - Newly unblocked: WP-6 (Packaging)

---
